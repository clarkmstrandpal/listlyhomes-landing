import { Link, NavLink } from "react-router-dom";

export default function Header(){
  return (
    <header className="header sticky top-0 bg-white z-50">
      <div className="mx-auto max-w-6xl px-4 h-16 flex items-center justify-between">
        {/* Left: Brand */}
        <Link to="/" className="flex items-center gap-2" aria-label="Listly Homes">
          <img src="/img/horzontal_logo.png" alt="Listly Homes" style={{height:"auto", maxHeight:"64px"}} />
        </Link>

        {/* Center: Nav */}
        <nav aria-label="Primary">
          <ul className="flex items-center gap-6">
            <li><NavLink to="/" end>Home</NavLink></li>
            <li><NavLink to="/pricing">Pricing</NavLink></li>
            <li><a href="#how">How it works</a></li>
          </ul>
        </nav>

        {/* Right: Actions */}
        <div className="flex items-center gap-3">
          {/* Per request, make Login go to the Dashboard */}
          <NavLink to="/dashboard" className="underline">Log in</NavLink>
          <a href="#form" className="px-3 py-2 rounded-md bg-black text-white">Get Started</a>
        </div>
      </div>
    </header>
  );
}
