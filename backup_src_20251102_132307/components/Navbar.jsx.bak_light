import React from "react";

export default function Navbar() {
  return (
    <header className="w-full bg-white border-b border-slate-200">
      <div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        {/* increase header height so big logo fits */}
        <div className="flex h-24 md:h-28 lg:h-32 items-center justify-between">
          {/* Brand */}
          <a href="/" className="flex items-center gap-4">
            <img
              src="/img/logo-horizontal.png"
              alt="BuyerBoard"
              className="h-20 md:h-24 lg:h-28 w-auto select-none"
              draggable="false"
            />
          </a>

          {/* Nav */}
          <nav className="hidden md:flex items-center gap-10 text-base">
            <a href="#how" className="text-slate-600 hover:text-slate-900">How it works</a>
            <a href="#buyer-form" className="text-slate-600 hover:text-slate-900">Buyer Form</a>
            <a href="#dashboard" className="text-slate-600 hover:text-slate-900">Dashboard</a>
          </nav>

          {/* CTA */}
          <div className="flex items-center gap-4">
            <a href="#login" className="hidden sm:inline text-slate-600 hover:text-slate-900">Log in</a>
            <a
              href="#get-started"
              className="rounded-full px-5 py-3 text-white shadow-sm bg-gradient-to-r from-[#0E57FF] to-[#14C6F1]"
            >
              Get Started
            </a>
          </div>
        </div>
      </div>
    </header>
  );
}
