import React from "react";
import Header from "../components/Header.jsx";

function Metric({num,cap,title,text}){
  return (
    <div className="card" style={{padding:20}}>
      <div style={{textAlign:"center"}}>
        <div style={{fontSize:28,fontWeight:800}}>{num}</div>
        <div style={{color:"#64748b",fontSize:13}}>{cap}</div>
      </div>
      <div style={{height:1,background:"#e5e7eb",margin:"14px 0"}} />
      <div style={{fontWeight:600}}>{title}</div>
      <div style={{color:"#475569",fontSize:14,marginTop:4}}>{text}</div>
    </div>
  );
}

function SimpleLeadForm(){
  return (
    <form className="card" style={{padding:16,display:"grid",gridTemplateColumns:"1fr 1fr",gap:12}}>
      <input placeholder="Full name" className="input" style={{border:"1px solid #e5e7eb",borderRadius:12,padding:"10px 12px"}}/>
      <input placeholder="Email" className="input" style={{border:"1px solid #e5e7eb",borderRadius:12,padding:"10px 12px"}}/>
      <input placeholder="Phone" className="input" style={{border:"1px solid #e5e7eb",borderRadius:12,padding:"10px 12px"}}/>
      <input placeholder="ZIP" className="input" style={{border:"1px solid #e5e7eb",borderRadius:12,padding:"10px 12px"}}/>
      <textarea rows="4" placeholder="Neighborhoods, timing, must-haves" style={{gridColumn:"1 / span 2",border:"1px solid #e5e7eb",borderRadius:12,padding:"10px 12px"}}></textarea>
      <div style={{gridColumn:"1 / span 2"}}>
        <button type="button" className="btn-primary">Submit</button>
      </div>
    </form>
  );
}

export default function Landing(){
  return (
    <div>
      <Header/>

      {/* HERO */}
      <section style={{padding:"36px 0"}}>
        <div className="container grid grid-2" style={{alignItems:"center"}}>
          <div>
            <h1 className="h1">Here to help you find <span style={{color:"var(--blue)"}}>real buyers</span> in your market.</h1>
            <p className="lead" style={{marginTop:12}}>
              We scan public sources for shopping intent and route by ZIP, budget and criteria.
            </p>
            <div style={{marginTop:18,display:"flex",gap:12,flexWrap:"wrap"}}>
              <a href="#pricing" className="btn-primary">Start Receiving Leads</a>
              <a href="#how" className="btn">How it works</a>
            </div>
          </div>
          <div className="card" style={{overflow:"hidden"}}>
            <img src="/img/hero-bg.jpg" alt="Hero" style={{height:360,objectFit:"cover",width:"100%"}} onError={e=>e.currentTarget.style.display="none"}/>
          </div>
        </div>
      </section>

      {/* METRICS */}
      <section id="how" style={{padding:"8px 0 24px"}}>
        <div className="container metrics">
          <Metric num="10K+" cap="Active buyer posts tracked" title="Verified & Local" text="Every lead is tied to your market and checked for real intent."/>
          <Metric num="4K+"  cap="Monthly agent matches"      title="Reply-Ready"      text="We score for 'ready to talk' and push to your CRM or inbox."/>
          <Metric num="30+"   cap="Markets live today"         title="Youre in Control" text="Set filters, pause anytime. Only pay for leads you want."/>
        </div>
      </section>

      {/* TESTIMONIALS */}
      <section style={{padding:"8px 0 24px"}}>
        <div className="container grid grid-2">
          <div className="card" style={{padding:18}}>
            Two listing appointments in 48 hours. <b>Katie C.</b>
          </div>
          <div className="card" style={{padding:18}}>
            Better ROI than my ad spend last month. <b>Andrew R.</b>
          </div>
        </div>
      </section>

      {/* BUYER FORM */}
      <section id="buyers" style={{padding:"16px 0 28px"}}>
        <div className="container">
          <h2 style={{fontSize:24,fontWeight:800,marginBottom:12}}>Looking for a home? Tell us what you want.</h2>
          <SimpleLeadForm/>
        </div>
      </section>

      {/* CTA STRIP */}
      <section id="pricing" style={{ background: "linear-gradient(90deg, var(--blue), var(--aqua))" }}>
        <div className="container" style={{color:"#fff",padding:"22px 0",display:"flex",flexWrap:"wrap",alignItems:"center",justifyContent:"space-between",gap:12}}>
          <div>
            <div style={{fontWeight:700,fontSize:18}}>Ready to meet real buyers?</div>
            <div style={{opacity:.85}}>Spin up filters and start matching today.</div>
          </div>
          <a className="btn" href="/signup" style={{background:"#fff",color:"#0E57FF",fontWeight:700}}>Get Started</a>
        </div>
      </section>

      <div className="footer"> {new Date().getFullYear()} Listly Homes. All rights reserved.</div>
    </div>
  );
}

